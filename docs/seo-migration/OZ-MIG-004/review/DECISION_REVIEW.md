# OZ-MIG-004 independent decision review

## Recommendation

**REVISE BEFORE APPROVAL.** The nine established production owners are appropriate, but the hash-bound plan does not specify a complete, atomic routing/publication transition. Do not approve or apply this exact manifest yet. The catalogue hold, existing Bamboo retirement, supplier separation and no-new-hub decisions are supported within the limits below. This review is not Vincent's approval.

## Candidate and authority

- Repository: `/Users/daibang/Projects/oz-timber-floor`.
- Actual branch: `main`; actual HEAD: `b406af8e64bc84d269711a88303ae0a378ac96ee`. Starting worktree was clean.
- Expected older HEAD: `d0c66b802e544aa1a807f91798ea284c31c0998b`. Descendant `1916b19733a90907aafe765089ffbe85c390ef2c` contains the safe repairs/evidence; `b406af8e` adds release records and redirect smoke checks. No checkout, fetch or Git write was performed.
- Exact `DECISION_PLAN.json` bytes reproduce SHA-256 `ab50628d77038ea331e94d253aa4735ee0405a05f5d1d785b8cd28bf5650514e`. This is not a hash of the Markdown, a normalised JSON serialisation, or the website package.
- Original `DECISION_PLAN.md` SHA-256: `481deab12c50c88ecc48cfe7ae2f1f8291968ce6729ea3abe577980f175ea49d`.
- Binding to the current candidate is established: the manifest is unchanged, the 13 whole-file protected inputs still match the declared starting commit, and the proposed 172 routes exactly match the current unresolved readiness set. Starting-commit metadata in older reports is not the actual current HEAD.
- The original task inventory matches the current Git delta exactly: **304 files, including 222 HTML files and 30 PNGs**, not the earlier 298-file claim. Review outputs are additional and are not counted as implementation files.

Only the four requested review deliverables were created in the repository. Original reports, decision files, historical baselines, source facts, code, redirects and publish package were preserved. No provider write, submission, indexing change or deployment occurred.

## 172 catalogue decisions

| Verdict | Routes |
| --- | ---: |
| SUPPORTED_AS_PROPOSED | 172 |
| REVISE_TREATMENT | 0 |
| NEEDS_SPECIFIC_EVIDENCE | 0 |
| Total unique routes | 172 |

Support means **reversible page-level noindex while retaining the page and enquiry access**, not deletion, product discontinuation, a permanent redirect, or certification that the current product identity is correct. The CSV gives a separate record for every route, including its original contaminated field values, present source identity, possible alternatives and evidence limits.

Independent checks found:

- All 172 have exactly one current product source record and local HTML owner; all are self-canonical, `index,follow`, and currently in the 943-URL sitemap. Their sitemap-removal list is exactly the same unique set as the noindex list.
- Fresh GETs found **172/172 production paths returning 404** and **172/172 Netlify paths returning 200**, with page-level `index,follow` but global `X-Robots-Tag: noindex, nofollow`. This supports a candidate-only classification alongside the original WordPress inventory, not proof that a URL never existed historically.
- None appears in the preserved WordPress URL inventory or the unfiltered GSC Pages export. None is a current redirect destination or an original/current target in the reconstructed protected cohort. There is no evidenced established production owner being removed by this limited hold.
- The original records contain truncated navigation/download text, mixed fields or misassigned specification fragments. Their Hardwood colour/range identities are not independently established by those imported records. Temporary quarantine is proportionate for these candidate-only-evidenced identities; **missing thickness by itself is not an adequate general noindex rule**.
- The fresh readiness classifier reports `empty-or-placeholder-thickness` for all 172. That explains the test failure, but the classifier is not independent supplier-identity or retirement evidence. The review uses the import provenance and route-history checks as well.
- Product schema is **already absent on all 172**. Keeping it absent is supported; it must not be counted as 172 newly completed removals. Proposed controls should preserve self-canonicals and allow crawling; robots.txt blocking is not a substitute.
- The source still retains 188 Hardwood-labelled records: 12 in the restored range, these 172, and four other already-controlled Herringbone records. No source record should be deleted as part of this approval. Current range membership is not proof that all older products are discontinued.
- 96 routes have at least one currently indexable same-colour/image candidate elsewhere in the local catalogue. **No exact replacement SKU is established merely by those matches.** Each alternative and its source/indexability is recorded in the CSV; no unrelated category fallback is approved.
- Seven proposed routes have incoming HTML links: the first six alphabetically have 286 source files each; Ancient Stone has six. These are not zero-link pages. Keeping 200/enquiry access avoids stranding those links. Review the misleading related-product promotion separately; this task does not authorise removing it. The other 165 have no incoming anchor in the scanned HTML set, not necessarily no JavaScript or external discovery path.

Before later reindexation or permanent remapping, obtain the exact supplier SKU/range/colour and image-identity evidence for the affected route. That evidence is not needed to hold an unverified candidate page out of production indexing, but it is needed to claim a verified product or exact replacement.

## Nine canonical owners: retain the choices, revise the exact transition

Fresh GETs independently confirm all nine old WordPress paths are 200/self-canonical. All nine newer candidate paths are 404 on production. Netlify resolves old paths to the current newer candidate owners. GSC metrics below are unfiltered historical Pages metrics, not current ranks.

| Proposed retained owner | Clicks | Impressions | Other incoming rules not explicitly retargeted | Protected cohort rules affected |
| --- | ---: | ---: | ---: | ---: |
| `/floor-levelling/` | 86 | 17060 | 0 | 0 |
| `/hybrid/` | 37 | 37931 | 133 | 96 |
| `/laminate/` | 16 | 27766 | 150 | 119 |
| `/engineered-timber-flooring/` | 7 | 8594 | 76 | 50 |
| `/timber-floor-installation/` | 6 | 10006 | 0 | 0 |
| `/commercial-flooring/` | 2 | 7128 | 1 | 0 |
| `/office-flooring/` | 1 | 1624 | 0 | 0 |
| `/solid-timber/` | 4 | 6791 | 38 | 19 |
| `/vinyl/` | 6 | 8569 | 15 | 13 |
| Total | | | **413** | **297** |

All nine receive `REVISE_TREATMENT` for implementation completeness, **not** for choosing the established owner. No evidence supports replacing an established production URL simply to add “Sydney”.

Material exceptions to the exact plan:

1. **Redirect dependencies:** 422 current rules target the nine candidate owners. The plan reverses/removes the nine old-owner rules but does not enumerate the other 413 target changes. Of those dependencies, 297 belong to the protected 632-rule cohort. “Exclude the cohort from bulk noindex” does not resolve these canonical-switch dependencies.
2. **Forced rewrite ordering:** each candidate route has an early `200!` rewrite to its flat HTML file. Appending the proposed `301!` does not supersede it. Specify the exact replacement/removal and insertion order.
3. **Variants/file shadowing:** the exact changes name trailing-slash aliases only, omitting slashless, `.html` and `/index.html` behavior. Existing files can shadow unforced rules. Enumerate the final treatment and direct owner for each relevant variant instead of relying on implicit Pretty URLs.
4. **Owner publication:** no old-owner file currently exists in the local publication inventory. Specify either an old-owner 200 rewrite to an existing published flat file with the revised canonical, or the exact new output and packaging allowlist changes. The current directory allowlist excludes these old-owner directory names. Merely reversing the redirect is not a publication contract.
5. **Atomic source/contracts:** enumerate internal hrefs, breadcrumbs/JSON-LD, canonical tags, sitemap, flat/directory twins, keyword ownership, relevant catalogue fallback controls, generators and release checks. `scripts/image-dimension-audit.mjs` owns performance route constants; merely listing its importing checker is incomplete. `build-public-package.mjs`, `seo-migration-hardening.mjs` and the applicable source generators also need an explicit disposition. Token occurrence counts are discovery aids, not a complete change list.

The in-memory review models exact/wildcard matching and unforced file shadowing, assuming the new owners are already served successfully. Appending aliases leaves all nine current forced rewrites active. Prepending them reproduces 280 two-redirect paths and 27 unmapped variants serving existing files in this limited model. This is **not a Netlify emulator**; native Pretty URL behavior may change individual traces. The complete source list and simulation assumptions are in the review manifest. No hypothetical graph was installed.

The revised contract must test final 200/self-canonical owners, every inbound rule/variant, no loops/chains/noindex targets, and preserved enquiry/attribution query parameters. Netlify documents first-match ordering, forced-file behavior and automatic query propagation for ordinary 200/301/302 rules; future deployment still requires actual provider verification. See [redirect overview](https://docs.netlify.com/manage/routing/redirects/overview/) and [redirect options](https://docs.netlify.com/manage/routing/redirects/redirect-options/).

## Protected cohort and Bamboo reconciliation

The historical cohort can be reconstructed from `_redirects` at `efdcebc4e77a5125c1896199c96a9fd13d983078` (parent of `35bf6a82`) joined to the exact controlled route keys at `35bf6a82`. Those control keys still equal the current keys. The full source/old-target/current-target membership is included in `REVIEW_MANIFEST.json`.

- **632 plain 301 rules reach 488 unique historical target pages**, not 632 unique pages.
- Four additional forced `301!` aliases bring the complete audit set to **636 rules / 490 unique historical targets**.
- They currently reach **43 distinct, page-level-indexable replacement targets**; none reaches the 172 proposed pages. Current target validity does not by itself prove semantic equivalence, which this review does not newly approve for every historical fallback.
- The 297 dependencies above all belong to the plain-301 cohort. Their owner transition must be explicit in the revised plan.

**Bamboo retirement remains supported and coverage is intact.** The 21 count is a Bamboo/Verdura source-name scan, not the complete retirement-rule set. Nine additional Stonewood rules are relevant. The full current set is 30 rules: 25 explicit legacy paths, one category wildcard, and four landing variants. The historical inventory contains 26 legacy URLs; `/product-category/bamboo/verdura-bamboo/` is covered by `/product-category/bamboo/*`. All 26 legacy URLs plus four landing variants were checked against current first-match rules and fresh Netlify GETs: every one returns a single 301 followed by 200 at `/hardwood-timber-flooring-sydney/`.

The replacement is the already-approved broad Hardwood alternative, not an assertion that Bamboo and Hardwood are the same product. No discovered inventory gap warrants reopening retirement policy or inventing an exact product replacement. Wildcard coverage beyond known historical URLs is a rule property, not evidence that arbitrary invented legacy products existed.

## Supplier separation and service-area scope

All six supplier pages are supported as separate: Hybrid, Laminate, Engineered Timber, Solid Timber, Vinyl and the broad Timber supplier page. Each has a distinct supplier H1, a different body from its paired category, and actual stock/batch/quantity/lead-time/supply-only enquiry content. All six expose the eight checked supply-intent signals; the decision is supported by their paragraphs, not just keyword counts. The broad timber supplier page explicitly helps customers choose between engineered and solid timber. Shared templates and vocabulary do not prove cannibalisation; GSC export absence would not establish it either.

No new indexable service-area hub is needed for continuity. The existing home/service pages retain Sydney supply/installation/preparation intent and Cabramatta showroom relevance. No specific lost established hub was evidenced. No suburb pages, geographic claims or new schema were added.

## Evidence quality and regression results

The unfiltered GSC workbook contains 388 unique Pages rows; filtered exports contain 59 (`+Sydney`) and 20 (`+timber flooring sydney`). All three report Web / Last 12 months, with Chart dates **2025-08-30 through 2026-08-29**, despite the 2026-09-01 filename. Overlapping exports were not summed. Missing route metrics remain `NOT_PRESENT_IN_EXPORT` with blank numeric fields. Historical average position is not current ranking. External-link exports have no target pairing: backlinks remain `UNKNOWN`, not zero.

The existing scripts were run unmodified. Commands that write reports ran in an isolated copy; the freeze was successfully recaptured against the real Git root using its supported temporary output directory. The first isolated freeze attempt lacked Git metadata and failed; its copied comparison was not accepted as a fresh capture. Full commands, exits and output digests are in `REVIEW_MANIFEST.json`.

| Check actually run | Result |
| --- | --- |
| `git diff --check` | PASS |
| `node scripts/current-release-contract.mjs --no-write` | PASS: 13 protected files, 222 changed HTML files, no unexplained protected drift |
| `node scripts/build-public-package.mjs --check` | PASS: protected preview; 6236 files; 583803198 bytes |
| `node scripts/catalogue-field-integrity.mjs` | PASS: 167 ranges, 1534 products, retained 12-colour range |
| `node scripts/analytics-event-contract.mjs` | PASS: local behavioral fixtures, no live GA4 |
| `node scripts/release-old-assets.mjs` | PASS: 2201 HTML files, zero old-host references |
| `node scripts/image-dimension-audit.mjs` in isolated copy | PASS: 15 owners, 170 images, 86 assets; no missing/mismatched dimensions |
| `node scripts/ui-ux-lcp-check.mjs` | PASS: ten specified physical LCP outputs |
| `node scripts/migration-readiness.mjs --allow-external` in isolated copy | NO-GO: exactly the 172 pending routes; zero additional high/medium findings; no broken internal links/assets |
| `python3 -B scripts/gsc_migration_audit.py` in isolated copy | PASS: 130 equity rows, 60 manual rows; eight unresolved cases remain reported |
| `node scripts/seo-migration-hardening.mjs` in isolated copy | PASS: 60 semantic contracts, 130 equity decisions, 12 keyword owners; 360 quality-review observations remain, not 360 new blockers |
| `node scripts/priority-route-performance-check.mjs` | FAIL: three stale flat-owner hashes |
| `node scripts/ui-qa.mjs` | FAIL: three stale browser evidence hashes |
| Temporary-output `ui-ux-seo-freeze.mjs capture-after` and `compare` | Fresh capture produced; both exit 1 because three old performance allowances no longer apply; comparison records 253 differences |
| 456 public GET route/site probes | Completed without HTTP transport errors after retry with permitted network access; 228 paths per site. No POST/form submission |

The package hash **`0eb7750b37126d38bc18f9f9c15e35b180b7bbf617e2fd44979657612a44c3be`** is the SHA-256 of the generated ordered file-manifest serialisation, not a ZIP hash. The current checker verifies the actual `dist` contents. The earlier 583801926-byte / `bd602b…` claim predates the final analytics-context repair; current output is 1272 bytes larger. Fresh Netlify `site.js` bytes match current local `site.js`. A complete remote-provider package manifest was not obtained, so full deployment byte equivalence is not claimed.

All 172 fetched catalogue HTML hashes differ from raw local files by an independently verified **536-byte hosting comment/metadata insertion**. Inspecting one representative response established the exact inserted bytes; inserting that same block at the same source position reproduces **172/172 previously fetched response hashes exactly**. This is evidence of that hosting transformation, not unexplained catalogue drift. The extra representative GET is additional to the 456 route/site probes. No provider setting was changed.

Current inventory: **2201 physical HTML; 971 physical indexable HTML; 943 canonical publication/sitemap URLs; 1230 physical noindex HTML representing 1228 routes; 350 redirect-only routes; 1912 rules.** Redirect-only is not an additional disjoint HTML count. Catalogue: 2115 pages, 893 currently indexable, 1222 already controlled; the classifier accepts 721 and flags 1394, including the pending 172. Hypothetically applying only the approved-list controls would yield 771 sitemap URLs; that has not happened. The retained historical reconciliation of 1973 sitemap entries is 1973 − 3 missing-image exclusions − 1026 catalogue exclusions − 1 Bamboo retirement = 943; 941 was the temporary erroneous omission of home/about, not today's count.

### Stale evidence and historical differences

There are two distinct three-hash failure sets, not three hashes covering everything:

- Old UI evidence: `assets/site.js`, `engineered-timber-flooring-sydney/index.html`, `floor-levelling-sydney/index.html` in `docs/ui-ux/generated/browser-qa.json`.
- Performance evidence: `floor-levelling-sydney.html`, `laminate-flooring-sydney.html`, `engineered-timber-flooring-sydney.html` in `docs/performance/generated/priority-route-performance.json`. The performance-only normalisation allowances also fail for those three owners.

After an authorised implementation, rerun real browser measurements for the full 14-route × 5-viewport UI matrix and its menu/catalogue/contact/dock interactions; then rerun `ui-qa.mjs`. Recapture the 15-route × 5-width performance matrix using actual publication owners, preserve the original baseline, generate a separately identified post-change report via `priority-route-performance-check.mjs --write --baseline <preserved-75-record-baseline> --final <fresh-75-record-capture> --base-url <read-only-fixture-url>`, and run its check. These placeholders identify evidence that must actually be captured, not files claimed to exist. The repository has a report writer/checker, not a one-command full historical browser recapture. Do not just replace expected hashes. Keep the old image-only allowance baseline intact; owner/content changes require explicit release-specific deltas, not permission to treat them as image-only changes.

The stored task-specific `OZ-MIG-004/BROWSER_QA.json` has eight current source hashes that still match, 25 reported cells and zero non-GET requests. All 30 referenced PNG hashes and dimensions verify. This is **retained prior execution evidence**, not fresh browser execution in this review, and its five-route matrix does not replace the full historical UI/performance matrices. Physical-device safe areas and live form delivery remain unverified.

Fresh old-baseline freeze differences reconcile as **1 source + 7 protected-file + 245 page-field = 253 records**, not 253 defects. The comparison actually committed at `d0c66b8` contains **31**, not 29: 1 branch provenance + 6 file + 24 page records. Thirty keys persist; the obsolete branch key disappears; 223 keys are added (commit provenance, catalogue source file, 221 page-field records). Two retained values change further: analytics JS and the floor-levelling HTML hash. New link-record changes include linked-card text cleanup, not unexplained href-target changes. The machine-readable review records every difference key and provenance class.

Current release allowances pin canonical/robots/form contracts, exact five malformed identity cleanups, and the explicit Hardwood range metadata/schema/link delta. In-memory negative probes reject a changed canonical and title. They do **not** constrain arbitrary plain body text: an inert body sentinel remains outside that field contract. Therefore the PASS is meaningful for protected-field safety but not proof of every factual statement or all visual behavior. No new unexplained protected-field change was found; no test was weakened.

## Separate readiness decisions and smallest next action

- **Local review:** complete; recommendation is revision of the exact transition plan, not permission to implement.
- **Protected preview:** local package validates and the requested current public route coverage was checked. Retained scoped browser evidence is hash-bound. Full UI/performance evidence is stale; complete remote package equality is unproven.
- **Production:** still NO-GO. The pending decisions, a fully specified canonical transition, fresh post-change regression evidence, real form delivery, live GA4 approval/configuration, physical-device checks and separate production-indexing/cutover authority remain release prerequisites.

**Single smallest next action:** revise the existing decision manifest only to enumerate the nine atomic owner transitions and their 413 incoming-rule dependencies (297 protected-cohort members), URL variants, rewrite ordering and exact owner-publication contracts; then present the new exact hash for owner approval. Preserve the supported 172-route hold, Bamboo retirement, six supplier owners and no-new-hub scope. Do not implement or deploy from this review.
